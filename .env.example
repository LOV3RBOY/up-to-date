# Copy this file to `.env` at the root of the repository and fill in the
# values.  The environment variables are loaded by the backend and worker
# services.  Do not commit your real secrets to version control.

# Database connection string.  The default points to the `db` service in
# docker-compose.yml.  You can override it to connect to a local database
# outside Docker.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/postgres

# Celery broker and result backend.  The default uses the `redis` service.
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Perigon News API key for fetching articles.  Sign up at https://perigon.io
# and paste your API key here.  Leave blank to disable the fetch task.
PERIGON_API_KEY=

# OpenAI API key for summarization and classification tasks.  Copy the
# value provided by your organisation and keep it secret.
OPENAI_API_KEY=

# Slack Incoming Webhook URL for sending alerts.  Create a webhook in
# Slack and paste the URL here.  Leave blank to disable Slack notifications.
SLACK_WEBHOOK_URL=
